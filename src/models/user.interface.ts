export interface UserState {
    id: number;
    role: string;
    email: string;
    name: string;
    password: string;
    oldPassword: string;
    updatedAt: string;
    createdAt: string;
    token: string;
}